# Evals

Use this skill when an agent needs to configure a ThinkFeel project with a Playground API key or inspect whether `THINKFEEL_API_KEY` is already available. Use `OPENAI_API_KEY` only for explicit OpenAI-compatible completions projects.

Do not use this skill for documentation-only questions or static UI work that will not call the Playground API.
